# Recommended Lead Form Contract
Use only if compatible with existing backend.

Common fields: `name`, `phone`, `email`, `message`, `website` (honeypot).

Metadata can include:
`form_name`, `page_url`, `page_title`,
`utm_source`, `utm_medium`, `utm_campaign`, `utm_term`, `utm_content`, `referrer`.

Frontend must never contain secrets.
Conversion event only after confirmed backend success.
